# Database Models

> Documentation for the database schema — coming soon.

The models live in `src/shared/db/models.py`. Tables: `tenants`, `calling_services`, `tenant_keys`, `ledger_entries`, `policies`.